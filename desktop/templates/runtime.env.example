# PT Nexus Desktop 运行时环境变量示例
# 首次启动后，本模板会自动生成用户目录下的 runtime.env。
# 建议直接编辑 runtime.env，本文件仅作示例备份。

# 数据库类型：sqlite / mysql / postgresql
DB_TYPE=sqlite

# ===== MySQL 示例（DB_TYPE=mysql 时启用） =====
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=pt_nexus

# ===== PostgreSQL 示例（DB_TYPE=postgresql 时启用） =====
# POSTGRES_HOST=127.0.0.1
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password
# POSTGRES_DATABASE=pt_nexus

# ===== 可选端口覆盖 =====
# SERVER_PORT=5275
# BATCH_PORT=5276
# UPDATER_PORT=5274

# app.py 内是否内嵌后台线程（desktop 默认 false，由 background_runner 独立运行）
# PTNEXUS_EMBED_BG_IN_APP=false

